apiVersion: v1
kind: Service
metadata:
  name: gongmcp-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gongmcp-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
